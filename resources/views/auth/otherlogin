<x-guest-layout>
    <x-authentication-card>
        <x-slot name="title">
        </x-slot>

        <x-validation-errors class="mb-4" />

        @if (session('status'))
            <div class="mb-4 font-medium text-sm text-green-600 dark:text-green-400">
                {{ session('status') }}
            </div>
        @endif

        <form method="POST" action="{{ route('login') }}" >
            @csrf

            <div class="input-group input-group-outline my-3">
                <x-label for="email" value="{{ __('Email') }}" class="form-label" />
                <x-input id="email" type="email" class="form-control" name="email" :value="old('email')" required autofocus autocomplete="username" />
            </div>

            <div class="input-group input-group-outline mb-3">
                <x-label class="form-label" for="password" value="{{ __('Password') }}" />
                <x-input id="password" type="password" class="form-control" name="password" required autocomplete="current-password" />
            </div>

            <div class="form-check form-switch d-flex align-items-center mb-3">
                <label for="remember_me" class="flex items-center form-check-label mb-0 ms-3">
                    <x-input type="checkbox" id="rememberMe" checked />
                    <span class="form-check-label mb-0 ms-3">{{ __('Recordarme') }} </span>
                </label>
            </div>

            <div class="flex items-center text-center justify-end mt-4">
                @if (Route::has('password.request'))
                    <a class="underline text-sm text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 rounded-md font-weight-bold focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 dark:focus:ring-offset-gray-800" href="{{ route('password.request') }}">
                        {{ __('Forgot your password?') }}
                    </a>
                @endif

                <x-button type="button" class="btn bg-gradient-dark w-100 my-4 mb-2">
                    {{ __('Ingresar') }}
                </x-button>
        </form>
    </x-authentication-card>
</x-guest-layout>
